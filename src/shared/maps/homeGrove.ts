import { canopyMap } from "./canopy.ts";
import type { MapData } from "./types.ts";

/** Temporary stub; full Home Grove follows from local L3 commit. */
export const homeGroveMap: MapData = {
  ...canopyMap,
  id: "home-grove",
  name: "Home Grove",
};
