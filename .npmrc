# Colyseus lists every transport and driver as a required peer, including uWebSockets.js
# fetched from GitHub over SSH, which breaks npm ci on machines without GitHub SSH keys.
# Peers are not auto-installed; the ones Bowdle uses are listed explicitly in package.json.
legacy-peer-deps=true
