import { sunTempleMap } from "./sunTemple.ts";
import type { MapData } from "./types.ts";

/** Temporary stub; full Wild Crossing follows from local L3 commit. */
export const wildCrossingMap: MapData = {
  ...sunTempleMap,
  id: "wild-crossing",
  name: "Wild Crossing",
};
