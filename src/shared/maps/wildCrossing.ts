import { sunTempleMap } from "./sunTemple.ts";
import type { MapData } from "./types.ts";

/** Stub until Masky push of full L3 maps. */
export const wildCrossingMap: MapData = {
  ...sunTempleMap,
  id: "wild-crossing",
  name: "Wild Crossing",
};
